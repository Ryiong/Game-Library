<Window x:Class="Game_Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Game_Library.Views"
        xmlns:ext="clr-namespace:Game_Library.Extensions"
        mc:Ignorable="d"
        FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Display"
        Title="Game Library" Height="730" Width="1051"
        MinHeight="525" MinWidth="668"
        AllowDrop="True"
        Drop="Window_Drop"
        PreviewDragOver="Window_PreviewDragOver"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="50"
                  UseAeroCaptionButtons="False"
                  GlassFrameThickness="0"
                  CornerRadius="14"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Border Background="#F5F5F7" CornerRadius="14" BorderBrush="#D2D2D7" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Background="#F0FCF8" CornerRadius="14,0,0,14" BorderBrush="#ffffff" BorderThickness="0,0,1,0" Panel.ZIndex="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="14" Direction="0" ShadowDepth="2" Opacity="0.14" Color="Black"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="65"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Title Button App -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="16,16,16,0" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource MacTrafficLightButtonStyle}" Background="#FF5F56" ext:ButtonProps.HoverBackground="#F74339" ext:ButtonProps.PressedBackground="#DB332a"  BorderThickness="1.5" BorderBrush="#cc4f46" Click="CloseButton_Click" Foreground="#E0443E">
                            <Control Template="{StaticResource IconQuit}" BorderThickness="2" Width="6" Height="6" Foreground="#7b0708"/>
                        </Button>
                        <Button Style="{StaticResource MacTrafficLightButtonStyle}" Background="#FAC146" ext:ButtonProps.HoverBackground="#F5B11D" ext:ButtonProps.PressedBackground="#F2A807" BorderThickness="1.5" BorderBrush="#d4a338" Click="MinimizeButton_Click">
                            <Control Template="{StaticResource IconMinimize}" BorderThickness="2" Width="8" Height="8" Foreground="#9c5512" VerticalAlignment="Center"/>
                        </Button>
                        <Button Style="{StaticResource MacTrafficLightButtonStyle}" Margin="0" Background="#27C93F" ext:ButtonProps.HoverBackground="#13D630" ext:ButtonProps.PressedBackground="#04C921" BorderThickness="1.5" BorderBrush="#369841" Click="MaximizeButton_Click">
                            <Control Template="{StaticResource IconMaximize}" BorderThickness="2" Width="8" Height="8" Foreground="#0d660f"/>
                        </Button>
                    </StackPanel>

                    <!-- Sidebar Content Navigation -->
                    <StackPanel Grid.Row="1">
                        <TextBlock Text="GAME LIBRARY" Style="{StaticResource MenuHeaderStyle}"></TextBlock>
                        <ListBox x:Name="SidebarMenu" Style="{StaticResource MenuListBoxStyle}" SelectedIndex="{Binding SidebarSelectedIndex, Mode=TwoWay}">
                            <!-- Flash Classics-->
                            <ListBoxItem>
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Control Grid.Column="0" Template="{StaticResource IconFlash}" Width="20" Height="20" Style="{StaticResource MenuItemIconStyle}"/>
                                    <TextBlock Grid.Column="1" Text="Flash Classics"  Style="{StaticResource MenuItemTextStyle}" VerticalAlignment="Center"/>
                                </Grid>
                            </ListBoxItem>
                            <!-- HTML5 Indie -->
                            <ListBoxItem>
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Control Grid.Column="0"  Template="{StaticResource IconHTML}" Width="20" Height="24" Style="{StaticResource MenuItemIconStyle}"/>
                                    <TextBlock Grid.Column="1"  Text="HTML5 Indie"  Style="{StaticResource MenuItemTextStyle}" VerticalAlignment="Center"/>
                                </Grid>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Column="1" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="55"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Header -->
                <Border Background="White" WindowChrome.IsHitTestVisibleInChrome="True" Grid.Row="0" BorderThickness="0,0,0,1" CornerRadius="0,14,0,0" BorderBrush="#E6F2ED">
                    <Grid>
                        <!-- Default Header -->
                        <Grid x:Name="DefaultHeaderGrid" Visibility="{Binding IsDefaultHeaderVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="AddGameButton" Grid.Column="0" Command="{Binding AddGameCommand}" Style="{StaticResource MacPrimaryButtonStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand" Margin="0,0,30,0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <Control Template="{StaticResource IconPlus}" Width="20" Height="19" Margin="5, 0"/>
                                        <TextBlock Text="Add Game" VerticalAlignment="Center" Margin="2,2,8,2" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            <Border Grid.Column="1" Height="34" MaxWidth="400" Background="#F2F2F7" CornerRadius="13" Margin="10,0" VerticalAlignment="Center">
                                <Grid Margin="10,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Control Grid.Column="0" Template="{StaticResource IconSearch}" Width="16" Height="16" Foreground="#8E8E93" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox Grid.Column="1" 
                                            Text="{Binding SearchKeyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                            Background="Transparent" BorderThickness="0" 
                                            VerticalAlignment="Center" FontSize="13" Foreground="#1C1C1E"/>
                                </Grid>
                            </Border>
                            
                        </Grid>

                        <!-- Header Detail -->
                        <Grid x:Name="DetailHeaderGrid" Visibility="{Binding IsDetailHeaderVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <!-- Return Button -->
                        </Grid>
                    </Grid>
                </Border>
                <!-- Content View -->
                <ContentControl x:Name="DynamicContentViewer" Grid.Row="1" Margin="0" Content="{Binding CurrentView}"/>
                <!-- Footer -->
                <Border Grid.Row="2" VerticalAlignment="Bottom" CornerRadius="0,0,14,0" Background="#E5F0EC" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="LogApp" Grid.Column="0"
                                   Text="{Binding LogText}" FontSize="15"
                                   HorizontalAlignment="Left" Margin="15,0,0,0"
                                   VerticalAlignment="Center"
                                   Padding="0,4,0,4" FontWeight="Medium"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="ServerStatus" Grid.Column="1"
                                   Text="{Binding StatusText}" FontSize="15"
                                   HorizontalAlignment="Right" Margin="0,0,15,0"
                                   Padding="0,4,0,4" FontWeight="Medium"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

            </Grid>
        </Grid>
    </Border>
</Window>
