<UserControl x:Class="Game_Library.Views.AllGamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ext="clr-namespace:Game_Library.Extensions"
             FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Display"
             mc:Ignorable="d" Background="White">
    <UserControl.Resources>
        <ext:ThumbnailConverter x:Key="ThumbConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel Margin="28,0">
            <!-- Recent Game -->
            <StackPanel Margin="0,25,0,0" x:Name="RecentSection" Visibility="Collapsed">
                <TextBlock Text="Recently Played" Foreground="#131E1B" FontSize="26" FontWeight="Bold" Margin="0,0,0,15"/>
                <Border Width="300" Height="225" HorizontalAlignment="Left" CornerRadius="20">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding Thumbnail, Converter={StaticResource ThumbConverter}}" Stretch="UniformToFill"/>
                    </Border.Background>
                    <Border Background="#59000000" CornerRadius="20">
                        <Grid>
                            <StackPanel Width="270" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="15,0,0,0">
                                <Border Background="#005B41" CornerRadius="10" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Type}" Foreground="White" FontSize="12" FontWeight="Medium"/>
                                </Border>
                                <TextBlock Text="{Binding Title}" Foreground="White" FontSize="22" FontWeight="Heavy" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding LastPlayedText}" Foreground="White" FontSize="12" Margin="0,5,0,0"/>
                                <Button x:Name="PlayAgainBtn" Width="120" Style="{StaticResource GlassStyle}" HorizontalAlignment="Left" Margin="0,10,0,20" Cursor="Hand">
                                    <StackPanel Orientation="Horizontal">
                                        <Control Template="{StaticResource IconPlay}" Width="16" Height="16" Foreground="White"/>
                                        <TextBlock Text="Play Again" VerticalAlignment="Center" Foreground="White" Margin="8,0,0,0" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Border>
            </StackPanel>

            <!-- All Game Layout -->
            <TextBlock Text="All Games" Foreground="#131E1B" FontSize="26" FontWeight="Bold" Margin="0,35,0,15"/>
            <ItemsControl x:Name="AllGamesLayout">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Margin="0,0,18,18" Width="180" Click="GameCard_Click" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <Border Height="140" CornerRadius="16" ClipToBounds="True">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding Thumbnail, Converter={StaticResource ThumbConverter}}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                    <Grid>
                                        <Border Background="White" CornerRadius="5" Padding="6,3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                                            <TextBlock Text="{Binding Type}" Foreground="#005B41" FontSize="9" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                                <TextBlock Text="{Binding Title}" Foreground="#131E1B" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,3" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding AddedDate}" Foreground="#3E4945" FontSize="10" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>