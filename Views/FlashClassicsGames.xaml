<UserControl x:Class="Game_Library.Views.FlashClassicsGames"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ext="clr-namespace:Game_Library.Extensions"
             FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Display"
             mc:Ignorable="d" Background="White">
    <UserControl.Resources>
        <ext:ThumbnailConverter x:Key="ThumbConverter"/>
    </UserControl.Resources>
    <StackPanel Margin="28,0">
        <TextBlock Text="Flash Classics Games" Foreground="#131E1B" FontSize="26" FontWeight="Bold" Margin="0,35,0,15"/>
        <ListView x:Name="GamesListView"
          ItemsSource="{Binding FilteredGames}"
          Background="Transparent"
          BorderThickness="0"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          VirtualizingPanel.IsVirtualizing="True"
          VirtualizingPanel.VirtualizationMode="Recycling"
          VirtualizingPanel.CacheLengthUnit="Page"
          VirtualizingPanel.CacheLength="1,1">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- CARD GAME DESIGN BUTTON -->
                    <Button Style="{StaticResource NonableButtonStyle}" 
                    Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                    CommandParameter="{Binding}"
                    Margin="0,0,15,15" Width="180" Cursor="Hand" 
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch">
                        <StackPanel>
                            <Border Height="140" CornerRadius="16" ClipToBounds="True" Width="145">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding Thumbnail, Converter={StaticResource ThumbConverter}}" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            <Grid>
                                <Border Background="White" CornerRadius="5" Padding="6,3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                                    <TextBlock Text="{Binding Type}" Foreground="#005B41" FontSize="9" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                            <TextBlock Text="{Binding Title}" Foreground="#005B41" FontSize="13" FontWeight="SemiBold" Margin="0,5,0,2" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Text"/>
                            <TextBlock Text="{Binding AddedDate}" Foreground="#3E4945" FontSize="10" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>