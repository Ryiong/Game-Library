<UserControl x:Class="Game_Library.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:Game_Library.Views"
             FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Display"
             mc:Ignorable="d" Unloaded="UserControl_Unloaded"
             Background="#D9D9D9"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E6F2ED" BorderThickness="0,0,0,1" Padding="15,0">
            <Grid>
                <Button x:Name="btnExitGame" 
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Center" 
                        Style="{StaticResource NonableButtonStyle}" 
                        Cursor="Hand" 
                        Click="btnExitGame_Click">
                    <Button.Content>
                        <Control Template="{StaticResource IconClose}" Width="22" Height="22" Foreground="#046B59"/>
                    </Button.Content>
                </Button>
                <TextBlock x:Name="lblPlayingGameTitle" 
                           Text="FLASH GAME PLAYER" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           FontSize="13"
                           FontWeight="Bold" 
                           Foreground="#046B59"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Background="#CCCCCC" SizeChanged="GameContainer_SizeChanged">
            <wfi:WindowsFormsHost x:Name="wfHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <wf:Panel x:Name="pnlGameContainer" BackColor="LightGray" />
            </wfi:WindowsFormsHost>
            <wv2:WebView2 x:Name="htmlWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed"/>
            <TextBlock x:Name="txtLoadingStatus" 
           Text="LOADING GAME..." 
           HorizontalAlignment="Center" 
           VerticalAlignment="Center" 
           FontSize="18" 
           FontWeight="SemiBold" 
           Foreground="#555555" 
           Visibility="Visible"/>
        </Grid>
    </Grid>
</UserControl>
