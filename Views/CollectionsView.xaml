<UserControl x:Class="Game_Library.Views.CollectionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Game_Library.Views"
             xmlns:ext="clr-namespace:Game_Library.Extensions"
             FontFamily="pack://application:,,,/Resources/Fonts/#SF Pro Display"
             Background="White">
    <UserControl.Resources>
        <ext:ThumbnailConverter x:Key="ThumbConverter"/>
    </UserControl.Resources>
    <Grid Margin="25,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header Action -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="COLLECTIONS" FontSize="25" FontWeight="Bold" Foreground="#046B59" VerticalAlignment="Center" Margin="10,0"/>
            <Button Grid.Column="1" Command="{Binding CreateCollectionCommand}" Style="{StaticResource MacPrimaryButtonStyle}" HorizontalAlignment="Right" Padding="12,6">
                <StackPanel Orientation="Horizontal">
                    <Control Template="{StaticResource IconPlus}" Width="16" Height="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Create New Collection" Margin="6,0,0,0" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Collection Cards Grid View -->
        <ListView Grid.Row="1" ItemsSource="{Binding FilteredCollections}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Width="210" Height="220" Margin="0,0,18,20" Background="#F8FAF9" CornerRadius="14" BorderBrush="#E5F0EC" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="145"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Click Card to Open Detail -->
                            <Button Grid.Row="0" Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource NonableButtonStyle}" Cursor="Hand">
                                <Border CornerRadius="14,14,0,0" ClipToBounds="True">
                                    <Image Source="{Binding Thumbnail, Converter={StaticResource ThumbConverter}}" Stretch="UniformToFill"/>
                                </Border>
                            </Button>

                            <!-- Card Bottom Title & Delete Button -->
                            <Grid Grid.Row="1" Margin="10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="13" Foreground="#131E1B" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding GameIds.Count, StringFormat='{}{0} Trò chơi'}" FontSize="11" Foreground="#046B59" FontWeight="SemiBold"/>
                                </StackPanel>

                                <Button Grid.Column="1" Command="{Binding DataContext.DeleteCollectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource NonableButtonStyle}" Cursor="Hand" ToolTip="Xóa Collection">
                                    <Control Template="{StaticResource IconDelete}" Width="18" Height="18" Foreground="#FF5F56"/>
                                </Button>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
